import type * as Layer from "effect/Layer";
import * as Logger from "effect/Logger";
/**
 * @category models
 * @since 1.0.0
 */
export interface Options {
    showFiberId: boolean;
    showTime: boolean;
    showSpans: boolean;
    enableColors: boolean;
}
/**
 * @category constructors
 * @since 1.0.0
 */
export declare const make: (options?: Partial<Options>) => Logger.Logger<unknown, void>;
/**
 * @category layers
 * @since 1.0.0
 */
export declare const layer: (options?: Partial<Options>) => Layer.Layer<never, never, never>;
//# sourceMappingURL=PrettyLogger.d.ts.map